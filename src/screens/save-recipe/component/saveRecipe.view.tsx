import React from "react";
import { View, Text } from "react-native";

interface saveRecipeProps {

}

export const SaveRecipeView = (props: saveRecipeProps) => {
    return (
        <View>
            <Text>React Native</Text>
        </View>
    )
}